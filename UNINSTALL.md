Instructions for uninstalling.  Useful for users who are done with this, and maintainers who want to test the install process.

```bash
claude plugins marketplace remove dmose-compare-figma-to-impl
claude mcp remove firefox-devtools --scope local
# maybe should jsut reomve the figma plugin and leave this here;
# XXX check claude default install to see default config
claude plugins marketplace remove claude-plugins-official

```